# 2026-07-02 工作日志

## 项目源码清理清单

- 对 CourseSelectionSystem 微服务项目进行全面扫描，识别可安全删除的文件
- 必须删除：所有模块的 target/（Maven 构建产物）、.codex-run/（WorkBuddy 运行时残留，数百MB）、.runtime/（微服务日志+PID）、.idea/（IDE 配置）
- 建议删除：HELP.md（Spring Initializr 占位文档，无实质内容）
- 可选删除：mvnw + mvnw.cmd + .mvn/（Maven Wrapper，取决于对方是否安装了 Maven）、.git/（版本历史）
- 确认保留：.gitignore、.editorconfig、.gitattributes、README.md、AGENTS.md、db/init.sql、docs/、scripts/、所有 pom.xml 和 src/
- 提供了 PowerShell 一键清理命令
- 执行清理：删除 9 个 target/ 目录、.codex-run/、.runtime/、.idea/、HELP.md，共 13 项
- 整理了数据库连接修改指引：6 个服务模块（user/course/selection/student/teacher/web-service）的 application.properties 中改 URL/用户名/密码 + db/init.sql 建库
